#include <stdio.h>

int main() {
    int x, y, z;
    
    printf("Ingrese la dimensión x: ");
    scanf("%d", &x);
    printf("Ingrese la dimensión y: ");
    scanf("%d", &y);
    printf("Ingrese la dimensión z: ");
    scanf("%d", &z);
    
    
    return 0;
}
